#include <doctest/doctest.h>
#include "kernels/linear_kernels.h"

using namespace ::FlexFlow;

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("forward_kernel CPU/GPU alignment (Linear)") {
    CHECK_MESSAGE(false, "TODO: forward_kernel CPU/GPU alignment (Linear)");
  }

  TEST_CASE("backward_kernel CPU/GPU alignment (Linear)") {
    CHECK_MESSAGE(false, "TODO: backward_kernel CPU/GPU alignment (Linear)");
  }
}
