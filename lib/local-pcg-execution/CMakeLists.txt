ff_add_library(
  NAME
    local-pcg-execution
  SRC_PATTERNS
    src/*.cc
  PUBLIC_INCLUDE
    include/
  PRIVATE_INCLUDE
    src/
  DEPS
    op-attrs
    utils
    kernels
    task-spec
    local-execution
    pcg
    spdlog
    compiler
)

add_subdirectory(test)
