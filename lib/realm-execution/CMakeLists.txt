ff_add_library(
  NAME
    realm-execution
  SRC_PATTERNS
    src/*.cc
  PUBLIC_INCLUDE
    include/
  PRIVATE_INCLUDE
    src/
  DEPS
    compiler
    kernels
    local-execution
    op-attrs
    pcg
    realm
    spdlog
    task-spec
    utils
)

add_subdirectory(test)
