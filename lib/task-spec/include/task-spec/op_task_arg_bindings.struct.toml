namespace = "FlexFlow"
name = "OpTaskArgBindings"
features = [
  "eq",
  "ord",
  "hash",
  "json",
  "fmt",
  "rapidcheck",
]

includes = [
]

src_includes = [
]

[[fields]]
name = "bindings"
type = "std::unordered_map<::FlexFlow::slot_id_t, ::FlexFlow::OpArgSpec>"
