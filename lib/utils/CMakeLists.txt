ff_add_library(
  NAME
    utils
  SRC_PATTERNS
    src/*.cc
  PUBLIC_INCLUDE
    include/
  PRIVATE_INCLUDE
    src/
  DEPS
    tl::expected
    fmt::fmt
    json
    CUDA::cudart
    rapidcheck
    libassert::assert
)

add_subdirectory(ffi)
add_subdirectory(test)
add_subdirectory(benchmark)
