#include <doctest/doctest.h>
#include "utils/containers/all_are_true.h"

using namespace ::FlexFlow;

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("all_are_true") {
    CHECK_MESSAGE(false, "TODO: all_are_true");
  }
}
