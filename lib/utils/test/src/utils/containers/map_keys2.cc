#include <doctest/doctest.h>
#include "utils/containers/map_keys2.h"

using namespace ::FlexFlow;

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("map_keys2") {
    CHECK_MESSAGE(false, "TODO: map_keys2");
  }
}
