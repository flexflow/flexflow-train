#include <doctest/doctest.h>
#include "utils/containers/map_values2.h"

using namespace ::FlexFlow;

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("map_values2") {
    CHECK_MESSAGE(false, "TODO: map_values2");
  }
}
