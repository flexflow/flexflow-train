#include <doctest/doctest.h>
#include "utils/containers/merge_disjoint_maps.h"

using namespace ::FlexFlow;

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("merge_disjoint_maps") {
    CHECK_MESSAGE(false, "TODO: merge_disjoint_maps");
  }
}
