#include <doctest/doctest.h>
#include "utils/orthotope/dim_projection.h"

using namespace ::FlexFlow;

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("compose_dim_projection") {
    CHECK_MESSAGE(false, "TODO: compose_dim_projection");
  }
}
